name: Publish Package

on:
  push:
    tags:
      - "v*"

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Checkout WaveKit
        uses: actions/checkout@v5
        with:
          repository: codewavebr/wavekit
          path: wavekit
          token: ${{ secrets.CODEWAVE_REPO_TOKEN }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install and build WaveKit
        working-directory: wavekit
        run: |
          bun install
          bun run build

      - name: Install WaveCalendar
        run: |
          bun install
          bun run link:wavekit

      - name: Test
        run: bun run test

      - name: Typecheck
        run: bun run typecheck

      - name: Build
        run: bun run build

      - name: Configure GitHub Packages registry
        run: echo "@codewave:registry=https://npm.pkg.github.com" > .npmrc

      - name: Publish to GitHub Packages
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm publish
